import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';



@NgModule({
  declarations: [],
  imports: [
    CommonModule
  ]
})
export class SharedModule { }

/**
 * contains components, directives, and pipes that are shared across multiple feature modules.
 * It allows for the centralization and reusability of common UI components, services, and other code throughout the application.
 * Examples of shared components may include buttons, input fields, modals, and tooltips.
 */
